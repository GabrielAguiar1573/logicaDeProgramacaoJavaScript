let contadora = 1
let soma = 0

while (contadora <= 100){
    soma += contadora /* le-se soma = soma + contadora */
    contadora++ /* Incremento */
    console.log (contadora)
}
console.log (`Soma dos cem primeiros número inteiro: ${soma}`)